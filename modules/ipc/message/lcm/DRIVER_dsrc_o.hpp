/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#include <lcm/lcm_coretypes.h>

#ifndef __DRIVER_dsrc_o_hpp__
#define __DRIVER_dsrc_o_hpp__

#include <string>


class DRIVER_dsrc_o
{
    public:
        std::string OID;

        std::string type;

        float      dx;

        float      dy;

        float      Angel;

        float      Speed;

        std::string att1;

        std::string att2;

    public:
        /**
         * Encode a message into binary form.
         *
         * @param buf The output buffer.
         * @param offset Encoding starts at thie byte offset into @p buf.
         * @param maxlen Maximum number of bytes to write.  This should generally be
         *  equal to getEncodedSize().
         * @return The number of bytes encoded, or <0 on error.
         */
        inline int encode(void *buf, int offset, int maxlen) const;

        /**
         * Check how many bytes are required to encode this message.
         */
        inline int getEncodedSize() const;

        /**
         * Decode a message from binary form into this instance.
         *
         * @param buf The buffer containing the encoded message.
         * @param offset The byte offset into @p buf where the encoded message starts.
         * @param maxlen The maximum number of bytes to reqad while decoding.
         * @return The number of bytes decoded, or <0 if an error occured.
         */
        inline int decode(const void *buf, int offset, int maxlen);

        /**
         * Retrieve the 64-bit fingerprint identifying the structure of the message.
         * Note that the fingerprint is the same for all instances of the same
         * message type, and is a fingerprint on the message type definition, not on
         * the message contents.
         */
        inline static int64_t getHash();

        /**
         * Returns "DRIVER_dsrc_o"
         */
        inline static const char* getTypeName();

        // LCM support functions. Users should not call these
        inline int _encodeNoHash(void *buf, int offset, int maxlen) const;
        inline int _getEncodedSizeNoHash() const;
        inline int _decodeNoHash(const void *buf, int offset, int maxlen);
        inline static uint64_t _computeHash(const __lcm_hash_ptr *p);
};

int DRIVER_dsrc_o::encode(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;
    int64_t hash = (int64_t)getHash();

    tlen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = this->_encodeNoHash(buf, offset + pos, maxlen - pos);
    if (tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int DRIVER_dsrc_o::decode(const void *buf, int offset, int maxlen)
{
    int pos = 0, thislen;

    int64_t msg_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &msg_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (msg_hash != getHash()) return -1;

    thislen = this->_decodeNoHash(buf, offset + pos, maxlen - pos);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int DRIVER_dsrc_o::getEncodedSize() const
{
    return 8 + _getEncodedSizeNoHash();
}

int64_t DRIVER_dsrc_o::getHash()
{
    static int64_t hash = _computeHash(NULL);
    return hash;
}

const char* DRIVER_dsrc_o::getTypeName()
{
    return "DRIVER_dsrc_o";
}

int DRIVER_dsrc_o::_encodeNoHash(void *buf, int offset, int maxlen) const
{
    int pos = 0, tlen;

    char* OID_cstr = (char*) this->OID.c_str();
    tlen = __string_encode_array(buf, offset + pos, maxlen - pos, &OID_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    char* type_cstr = (char*) this->type.c_str();
    tlen = __string_encode_array(buf, offset + pos, maxlen - pos, &type_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->dx, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->dy, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->Angel, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_encode_array(buf, offset + pos, maxlen - pos, &this->Speed, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    char* att1_cstr = (char*) this->att1.c_str();
    tlen = __string_encode_array(buf, offset + pos, maxlen - pos, &att1_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    char* att2_cstr = (char*) this->att2.c_str();
    tlen = __string_encode_array(buf, offset + pos, maxlen - pos, &att2_cstr, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    return pos;
}

int DRIVER_dsrc_o::_decodeNoHash(const void *buf, int offset, int maxlen)
{
    int pos = 0, tlen;

    int32_t __OID_len__;
    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &__OID_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__OID_len__ > maxlen - pos) return -1;
    this->OID.assign(((const char*)buf) + offset + pos, __OID_len__ - 1);
    pos += __OID_len__;

    int32_t __type_len__;
    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &__type_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__type_len__ > maxlen - pos) return -1;
    this->type.assign(((const char*)buf) + offset + pos, __type_len__ - 1);
    pos += __type_len__;

    tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->dx, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->dy, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->Angel, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    tlen = __float_decode_array(buf, offset + pos, maxlen - pos, &this->Speed, 1);
    if(tlen < 0) return tlen; else pos += tlen;

    int32_t __att1_len__;
    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &__att1_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__att1_len__ > maxlen - pos) return -1;
    this->att1.assign(((const char*)buf) + offset + pos, __att1_len__ - 1);
    pos += __att1_len__;

    int32_t __att2_len__;
    tlen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &__att2_len__, 1);
    if(tlen < 0) return tlen; else pos += tlen;
    if(__att2_len__ > maxlen - pos) return -1;
    this->att2.assign(((const char*)buf) + offset + pos, __att2_len__ - 1);
    pos += __att2_len__;

    return pos;
}

int DRIVER_dsrc_o::_getEncodedSizeNoHash() const
{
    int enc_size = 0;
    enc_size += this->OID.size() + 4 + 1;
    enc_size += this->type.size() + 4 + 1;
    enc_size += __float_encoded_array_size(NULL, 1);
    enc_size += __float_encoded_array_size(NULL, 1);
    enc_size += __float_encoded_array_size(NULL, 1);
    enc_size += __float_encoded_array_size(NULL, 1);
    enc_size += this->att1.size() + 4 + 1;
    enc_size += this->att2.size() + 4 + 1;
    return enc_size;
}

uint64_t DRIVER_dsrc_o::_computeHash(const __lcm_hash_ptr *)
{
    uint64_t hash = 0xdf11ec5b3d892db7LL;
    return (hash<<1) + ((hash>>63)&1);
}

#endif
